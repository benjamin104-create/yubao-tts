# 專案：語寶圖鑑

## 現況
- 後端：`yubao-tts`（FastAPI TTS 閘道 + /scan 拍照辨識 + /translate 翻譯）。
- 待修（2026-06-13 掃描發現）：
  - render.yaml 環境變數寫成 ANTHROPIC_API_KEY，程式讀的是 GEMINI_API_KEY（會讓 /scan 壞掉）。
  - /scan、/translate 無驗證/限流，金鑰可能被白嫖。
  - ADMIN_TOKEN 預設 "changeme"。

## 短影音計畫
- 開場動畫 demo 已用 HyperFrames 做出來（🦜 + 語寶圖鑑）。
- 下一步：A-Roll（HeyGen 數字人解說）+ B-Roll + 字幕，照六步驟流程。

## 下一步
- [ ] 決定是否先修 yubao-tts 的三個上線問題
- [ ] 把 Agent 搬到本機
