# AI 生成的原圖放這裡

流程與提示詞見 `docs/art_upgrade_web.md`。

- 直接放模型輸出的大圖（1024×1024 或更大），**不要**自己先縮小
- 命名：`<類別>_batch<N>.png`，例如 `mon_batch1.png`、`item_herbs.png`
- 轉檔由 `tools/pixelize.py` 做，產出放 `web/art/`

這個資料夾是「原料」，不是「成品」——成品不要手動放這裡。
